import { IConfig } from './iconfig';

export const Config: IConfig = {
  SqlInstance: "#{SqlInstance}",
  SqlDatabase: "#{SqlDatabase}",
  MQLiteUrl: "#{MQLiteUrl}",
  ScheduledQuerySql: "#{ScheduledQuerySql}"
}